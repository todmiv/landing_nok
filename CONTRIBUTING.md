# Вклад в проект "НОК в строительстве и архитектуре"

Спасибо за интерес к проекту! Мы приветствуем вклад от сообщества разработчиков, дизайнеров и специалистов в области строительства.

## 🚀 Как внести вклад

### 1. Подготовка к работе

#### Необходимые инструменты:
- **Git** - система контроля версий
- **Node.js** (опционально) - для дополнительных инструментов
- **VS Code** - рекомендуемый редактор кода
- **Браузер** - для тестирования (Chrome, Firefox, Safari, Edge)

#### Настройка локального окружения:
```bash
# Клонирование репозитория
git clone https://github.com/todmiv/landing_nok.git
cd landing_nok

# Запуск локального сервера
python -m http.server 8000
# или
php -S localhost:8000
```

### 2. Процесс внесения изменений

#### Шаг 1: Создание issue
Прежде чем вносить изменения, создайте issue в репозитории:
- Опишите проблему или улучшение
- Укажите метки (bug, enhancement, documentation, etc.)
- Обсудите изменения с командой проекта

#### Шаг 2: Создание ветки
```bash
# Создание ветки для вашей задачи
git checkout -b feature/your-feature-name
# или для исправления ошибки
git checkout -b fix/issue-number-description
```

#### Шаг 3: Внесение изменений
- Следуйте стандартам кода проекта
- Добавляйте комментарии к сложным участкам кода
- Тестируйте изменения на разных устройствах и браузерах
- Обновляйте документацию при необходимости

#### Шаг 4: Commit изменений
```bash
# Добавление файлов
git add .

# Коммит с понятным сообщением
git commit -m "feat: добавить новую функциональность

- Добавлено описание изменений
- Исправлена проблема с X
- Улучшена производительность Y"
```

#### Шаг 5: Создание Pull Request
```bash
# Отправка изменений в ваш форк
git push origin feature/your-feature-name

# Создание Pull Request через интерфейс GitHub
```

## 📝 Стандарты кода

### HTML
- Используйте семантические теги HTML5
- Добавляйте alt-тексты ко всем изображениям
- Соблюдайте правильную вложенность элементов
- Используйте data-атрибуты для JavaScript

```html
<!-- Хорошо -->
<button class="btn btn-primary" data-action="submit" aria-label="Отправить форму">
  <i class="fas fa-paper-plane" aria-hidden="true"></i>
  Отправить
</button>

<!-- Плохо -->
<div onclick="submitForm()" class="button">Отправить</div>
```

### CSS
- Используйте CSS Custom Properties (переменные)
- Следуйте методологии BEM для именования классов
- Избегайте глубокого каскадирования
- Добавляйте комментарии к сложным селекторам

```css
/* Хорошо */
:root {
  --primary-color: #2563eb;
  --transition: all 0.3s ease;
}

.hero__title {
  color: var(--primary-color);
  transition: var(--transition);
}

/* Плохо */
.header .nav .menu .item .link {
  color: blue;
  transition: all 0.3s ease;
}
```

### JavaScript
- Используйте современный ES6+ синтаксис
- Добавляйте JSDoc комментарии к функциям
- Используйте const/let вместо var
- Обрабатывайте ошибки корректно

```javascript
/**
 * Валидация формы
 * @param {HTMLFormElement} form - Элемент формы
 * @returns {boolean} Результат валидации
 */
function validateForm(form) {
  try {
    const inputs = form.querySelectorAll('input[required]');
    let isValid = true;

    inputs.forEach(input => {
      if (!input.value.trim()) {
        input.classList.add('is-invalid');
        isValid = false;
      } else {
        input.classList.remove('is-invalid');
        input.classList.add('is-valid');
      }
    });

    return isValid;
  } catch (error) {
    console.error('Ошибка валидации формы:', error);
    return false;
  }
}
```

## 🧪 Тестирование

### Перед отправкой PR:
- [ ] Проверьте работоспособность на мобильных устройствах
- [ ] Протестируйте в основных браузерах (Chrome, Firefox, Safari, Edge)
- [ ] Убедитесь, что формы отправляют данные корректно
- [ ] Проверьте валидацию форм
- [ ] Протестируйте все интерактивные элементы

### Автоматическое тестирование:
```bash
# Запуск линтера (если настроен)
npm run lint

# Проверка HTML валидности
# Используйте W3C Validator: https://validator.w3.org/

# Проверка CSS валидности
# Используйте W3C CSS Validator: https://jigsaw.w3.org/css-validator/
```

## 📚 Типы изменений

### 🎯 Возможные улучшения:

#### Функциональность
- Добавление новых разделов
- Улучшение форм и валидации
- Интеграция с новыми сервисами
- Добавление интерактивных элементов

#### Дизайн и UX
- Улучшение адаптивности
- Добавление новых анимаций
- Оптимизация цветовой схемы
- Улучшение типографики

#### Производительность
- Оптимизация загрузки изображений
- Минификация CSS/JS
- Улучшение Core Web Vitals
- Кеширование ресурсов

#### SEO и доступность
- Добавление структурированных данных
- Улучшение навигации с клавиатуры
- Добавление ARIA-атрибутов
- Оптимизация мета-тегов

#### Документация
- Улучшение README.md
- Добавление примеров кода
- Создание руководств пользователя
- Перевод документации

## 🚨 Важные правила

### ❌ Что нельзя делать:
- Отправлять спам или нерелевантные изменения
- Нарушать существующий дизайн и функциональность
- Добавлять вредоносный код
- Игнорировать отзывы ревьюверов
- Создавать конфликты в команде

### ✅ Что поощряется:
- Качественный, читаемый код
- Полное тестирование изменений
- Активное участие в обсуждениях
- Помощь другим контрибьюторам
- Следование лучшим практикам

## 📞 Контакты

При возникновении вопросов:
- Создайте issue в репозитории
- Напишите в раздел Discussions
- Свяжитесь с командой проекта

## 🙏 Благодарность

Спасибо за ваш вклад в развитие проекта! Ваши усилия помогают сделать сайт лучше для всех пользователей.

---

**Счастливого кодинга! 🚀**
